<div class="all-view">
  <div class="title">
    <h2>お気に入り商品一覧</h2>
  </div>
  <% if @items.present? %>
    <div class="subtitle">
      <h5>お気に入り商品 <%= @items_count %> 件</h6>
    </div>
    <%= render 'public/items/item', items: @items %>
    <%= paginate  @items %>
  <% else %>
    <div class="subtitle">
      <h5>お気に入りの商品はありません</h5>
    </div>
  <% end %>
</div>