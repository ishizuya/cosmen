<h1>商品詳細</h1>
  <%= image_tag @item.get_image(200, 200) %>
  <p>ブランド：<%= @item.brand %></p>
  <p>商品名：<%= @item.name %></p>
  <p>ジャンル：<%= @item.genre.name %></p>
  <p>税込価格(税抜価格)：<%= @item.add_tax_price %>(<%= @item.price.to_s(:delimited) %>)円</p>
  <p>容量：<%= @item.capacity %></p>
  <p>商品説明：<%= @item.introduction %></p>
  <div>
    <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success mt-4" %>
  </div>
  <div>
    <%= link_to "削除する", admin_item_path(@item.id), method: :delete, data: {confirm: "削除しますか？"} %>
  </div>